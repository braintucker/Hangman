import { Component, Input } from '@angular/core';

@Component({
  selector: 'word-edit',
  template: `
    <div class="panel panel-default">
      Add edit content...
    </div>
  `
})
export class WordEditComponent {


}
